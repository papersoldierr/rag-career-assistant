# Python
.venv/
venv/
__pycache__/
*.pyc

# Secrets — NEVER commit real keys
.env

# Local Postgres data (created by docker-compose)
pgdata/

# Personal source documents — feed the app locally, never commit (may hold PII)
documents/*
!documents/.gitkeep

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
